<%# rails adds the underscore before the name and the html.erb at the end automatically %>
<%# Since there is data, form_for will return prepolated values in the text areas.%>
<%= render 'form', post: @post %>